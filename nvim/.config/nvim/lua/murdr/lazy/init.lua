-- Empty on purpose. Using lazy.nvim's import mechanism (see lazy_init.lua)
-- Every file in this directory (besides this one) will be imported automatically
-- via: spec = { { import = 'murdr.lazy' } }
return {}
